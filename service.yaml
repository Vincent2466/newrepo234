apiVersion: v1
kind: Service
metadata:
  name: hasan
  labels:
    run: hasan
spec:
  type: NodePort
  ports:
  - port: 5000
    targetPort: 80
    protocol: TCP
    name: http
 
  selector:
    run: hasan
